DATABASE_URL="file:./dev.db"
NEXTAUTH_SECRET="your-secret-here-at-least-32-chars"
NEXTAUTH_URL="http://localhost:3000"

# Docker: DATABASE_URL is overridden by docker-compose to
# file:/app/data/dev.db so the database persists on a named volume
# across container restarts.  Everything else is read via env_file.
